# KIMNAMYONG — 포트폴리오 사이트 기획서

> repo: `KIMNAMYONG` / 문서 v0.1

---

## 0. 컨셉 한 줄

**"백지 위에 글자만."**
UI는 존재를 감추고, 필요할 때만 왼쪽에서 스며 나온다. 화면의 기본 상태는 언제나 *읽는 상태*다.

이 원칙 하나가 아래 모든 결정의 기준이 된다.

---

## 1. 정보 구조 (1 Depth)

| 메뉴 | 성격 | 콘텐츠 | 작성 경로 |
|---|---|---|---|
| **일** | UX 포트폴리오 | 프로젝트 케이스 (이미지 + 텍스트) | 파일(MDX) |
| **글** | UX라이팅 / 카피라이팅 | 텍스트 중심 원고 | 파일(MDX) |
| **삶** | 매일의 단상·일기 | 짧은 글, 날짜 단위 | **Admin CRUD** |

- `/` → 진입 시 **글**로 바로 착지(또는 최근 글 1편). 별도 랜딩 페이지를 두지 않는다. 백지 컨셉에 랜딩은 사족.
- `/work`, `/writing`, `/life` 3개 라우트. 상세는 `/writing/[slug]` 형태.
- **일/글/삶 메뉴 자체도 좌측 드로어 안에 산다.** 상단 GNB를 만들지 않는다 — 화면에 상시 노출되는 UI는 0개.

---

## 2. `글` 화면 설계 (핵심)

### 2.1 요구사항 정리

1. 백지 한가운데에 글(텍스트)만
2. 마우스를 화면 왼쪽 끝까지 → 글 제목 리스트만 노출
3. 리스트 영역에서 마우스가 벗어나면 → 메뉴 사라지고 다시 글만

이건 **엣지 트리거 드로어(edge-triggered drawer)** 패턴이다. 잘 만들면 마법 같고, 튜닝을 놓치면 "메뉴가 자꾸 튀어나오는 사이트"가 된다. 아래 수치가 그 경계다.

### 2.2 상태 기계

```
┌──────────┐  X < 16px, 120ms 유지   ┌──────────┐
│   REST   │ ──────────────────────▶ │   PEEK   │
│ 백지+본문 │                          │ 리스트 등장 │
└──────────┘ ◀────────────────────── └──────────┘
     ▲        X > 300px, 200ms 유지        │ 포인터 진입
     │                                     ▼
     │                               ┌──────────┐
     └───────────────────────────────│  BROWSE  │
              클릭 → 본문 교체 후 복귀    │ 제목 탐색  │
                                     └──────────┘
```

| 상태 | 중앙 본문 | 좌측 드로어 |
|---|---|---|
| `REST` | opacity 1 | 없음 (좌측 엣지에 2px 세로선, opacity .12 — 힌트) |
| `PEEK` | opacity **.2** + blur 1px | 슬라이드 인 (260ms) |
| `BROWSE` | opacity .2, hover한 제목의 글로 크로스페이드 프리뷰 | 제목 hover 상태 |
| `REST` 복귀 | opacity 1 | 슬라이드 아웃 (200ms) |

> **본문을 흐리는 이유:** 사용자가 말한 "리스트만 노출"을 문자 그대로 본문을 *없애면* 방금 읽던 위치를 잃는다. 흐리게 물러나 있으면 "리스트가 주인공"이라는 목적은 달성하면서 맥락은 남는다.

### 2.3 튜닝 수치 (그대로 구현값으로 쓰면 됨)

```
열림 임계값     X ≤ 16px        (화면 좌측 끝)
열림 지연       120ms           (스쳐 지나가는 마우스 무시)
닫힘 임계값     X ≥ 300px       (드로어 폭 260 + 여유 40)
닫힘 지연       200ms           (손떨림 관용 — 열림보다 반드시 길게)
드로어 폭       260px
전환 이징       cubic-bezier(.22,1,.36,1)
```

**히스테리시스가 핵심이다.** 여는 경계(16px)와 닫는 경계(300px)를 다르게 둬야 경계선 위에서 깜빡이지 않는다. 같은 값을 쓰면 반드시 떨린다.

### 2.4 리스트 안쪽

```
┌─ 260px ────────────────┐
│                        │
│  일   글   삶           │ ← 섹션 전환 (현재 섹션만 진하게)
│                        │
│  ────────────────      │
│                        │
│  철이 되면 철새로        │ ← 현재 글: 앞에 짧은 대시(—)
│  당신의 계절            │
│  말이 남는 자리          │
│  ...                   │
│                        │
│                        │
│  KIM NAM YONG          │ ← 하단 고정, 아주 작게
└────────────────────────┘
```

- **제목만.** 요약문·썸네일·태그 없음. 연도와 매체는 hover 시에만 우측 끝에 회색으로 스윽.
- 정렬: 최신순 기본. 글이 30편을 넘어가면 그때 카테고리(UX라이팅 / 카피 / 에세이) 소제목을 넣는다. 지금은 넣지 않는다.
- hover = 프리뷰, click = 확정. hover만으로 URL을 바꾸지 않는다(히스토리 오염).

### 2.5 중앙 본문 조판

읽기가 유일한 목적이므로 조판이 곧 디자인이다.

```
최대 폭      680px  (한글 기준 한 줄 33~38자)
본문 크기    17px / line-height 1.85
제목         28px, 본문과 96px 간격
자간         -0.01em (한글은 살짝 좁혀야 정갈)
여백         상하 15vh 이상 — 글이 화면에 '떠 있게'
색           #111 on #FFF (다크: #E8E6E3 on #0E0E0E)
서체         본문 Pretendard / 제목만 세리프(예: 나눔명조·source-han-serif) 대비
```

- 스크롤바 숨김(`scrollbar-width:none`). 대신 상단에 1px 진행 바.
- 글 끝에서 스크롤을 더 내리면 다음 글로 이어지는 부드러운 연결(선택 기능, 2차).

### 2.6 hover가 없는 환경 — 반드시 필요한 대체 경로

이 기획의 최대 리스크. **모바일에는 마우스가 없다.**

| 환경 | 진입 방법 |
|---|---|
| 모바일/태블릿 | 좌측 엣지 스와이프-인 + 좌상단 12px 인디케이터(탭 가능) |
| 키보드 | `Tab` 첫 포커스가 드로어 토글 / `Esc` 닫기 |
| 스크린리더 | 드로어는 `aria-expanded` 를 가진 실제 `<nav>`, 항상 DOM에 존재(visibility로만 제어) |
| 모션 민감 | `prefers-reduced-motion` 시 슬라이드 제거, 페이드 100ms만 |

---

## 3. `일`, `삶` 화면 (2차 — 방향만)

- **일**: 같은 드로어를 공유하되, 중앙이 텍스트 대신 케이스 스터디. 이미지가 들어오는 순간 백지 컨셉이 깨지므로 — 이미지는 **본문 폭(680px)을 넘지 않게** 묶어두는 걸 권한다.
- **삶**: 날짜가 곧 제목. 드로어 리스트가 `2026.08.22` 같은 날짜 목록이 된다. 무한 스크롤 단일 페이지도 대안.

---

## 4. 기술 구성

```
Next.js 15 (App Router) + TypeScript
Tailwind CSS  — 토큰만 쓰고 유틸 남발 금지
Framer Motion — 드로어 전환
MDX           — 일 / 글 원고 (git이 곧 백업)
Supabase      — 삶 + Admin 인증/CRUD
Vercel        — 배포, kimnamyong.com 연결
```

**왜 이 조합인가:** 일·글은 원고가 자주 안 바뀌니 파일로 두는 게 가장 빠르고 안전하다. 삶만 매일 쓰므로 DB와 Admin이 필요하다. 전부 DB에 넣으면 글 한 편 고치는 데 로그인해야 한다.

### 폴더 구조

```
KIMNAMYONG/
├─ app/
│  ├─ layout.tsx            # 드로어 + 본문 셸
│  ├─ page.tsx              # → /writing 리디렉트
│  ├─ work/[slug]/
│  ├─ writing/[slug]/
│  ├─ life/
│  └─ admin/                # 삶 작성 (인증 필요)
├─ components/
│  ├─ EdgeDrawer.tsx        # 2.2~2.3 상태 기계
│  ├─ TitleList.tsx
│  └─ Prose.tsx             # 2.5 조판
├─ content/
│  ├─ work/*.mdx
│  └─ writing/*.mdx
└─ lib/
```

### 원고 frontmatter

```yaml
---
title: 철이 되면 철새로
year: 2024
category: 에세이        # 에세이 | UX라이팅 | 카피
outlet: OO공모전         # 매체·클라이언트 (선택)
award: 우수상            # (선택)
order: 1
---
```

---

## 5. 만드는 순서

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 조판 확정 (2.5) — 글 1편을 백지에 올리고 폰트/여백만 잡기 | 읽는 게 편하다 |
| 2 | `EdgeDrawer` 상태 기계 + 히스테리시스 | 깜빡이지 않는다 |
| 3 | MDX 파이프라인, 글 전체 이관 | 리스트가 자동 생성된다 |
| 4 | 모바일 스와이프 / 키보드 대체 경로 | hover 없이도 다 된다 |
| 5 | `일` 섹션 | |
| 6 | Supabase + Admin, `삶` | 매일 쓸 수 있다 |
| 7 | 도메인·OG·메타 | |

**1단계와 2단계 사이에서 승부가 난다.** 나머지는 반복 작업이다.

---

## 6. 열린 질문

- 진입 시 첫 화면에 띄울 글: 최신글 / 대표작 고정 / 매번 랜덤 — 어느 쪽?
- 다크모드: 시스템 따라감 / 라이트 고정 — 백지 컨셉은 라이트 고정도 설득력 있음
- `일` 케이스에 들어갈 이미지 분량(백지 컨셉 유지 가능 여부를 좌우)
